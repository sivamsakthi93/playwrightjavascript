let a ="javascript";
let b = new String("javascript");
let c = new String(a);

//length property is used to get the length of a string
console.log(a.length);

//indexOf() method is used to get the index of a string
console.log(a.indexOf("j"));

//concat() method is used to concatenate two strings
console.log(a.concat(" is a programming language"));

//includes() method is used to check if a string contains a specified value
console.log(a.includes("java"));
